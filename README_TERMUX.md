# Build

This source tree is ready to be used once the Geode SDK/CLI and Android toolchain are installed.

From the project directory:

    geode build -p android64

The output `.geode` package is normally placed under `build-android64/`.

The project does not include a copied paid-mod DLL or its private `.tblib` data.
