# طريقة البناء (Build)

هذا المود جاهز بالكود، لكن يحتاج يتبنى (compile) على جهازك — ما ينبنى داخل المحادثة لأن Claude ما عنده اتصال إنترنت لتحميل Geode SDK / Android NDK.

## الطريقة الأسهل (سكربت جاهز)

```bash
chmod +x build.sh
./build.sh
```

السكربت يسوي تلقائيًا:
1. يحمّل/يحدّث Geode SDK.
2. يضبط CMake.
3. يبني المشروع.
4. ينسخ ملف `.geode` الناتج بجانب السكربت.

## قبل التشغيل، لازم يكون عندك مثبّت:

- **git**
- **cmake** (نسخة 3.21+)
- **مترجم C++20** (clang أو g++ حديث)
- (اختياري، للبناء لأندرويد) **Android NDK**، وتحدد مساره بمتغير البيئة:
  ```bash
  export ANDROID_NDK_ROOT=/path/to/android-ndk
  ```
  إذا ما حددته، السكربت يبني نسخة سطح مكتب بدلاً من أندرويد.

### على Termux (أندرويد مباشرة)
راجع `README_TERMUX.md` الموجود بنفس المجلد، فيه خطوات تثبيت الأدوات المطلوبة داخل Termux قبل تشغيل `build.sh`.

### على Windows
شغّل نفس الخطوات جوا **Git Bash** أو **WSL** (السكربت bash).

## بعد نجاح البناء

انسخ ملف `.geode` الناتج إلى مجلد مودات Geode عندك:
- أندرويد: `Android/data/.../Geode/mods` (المسار يعتمد على نسخة اللعبة/الجهاز)
- ثم شغّل Geometry Dash وفعّل المود من قائمة Geode.

## إذا صار خطأ أثناء البناء

انسخ رسالة الخطأ كاملة وابعثها، خصوصًا لو الخطأ متعلق بأسماء حقول زي:
`m_editorUI`, `m_selectedObjects`, `m_objectID`, `m_editorLayer`, `createObject`

هذي الأسماء معلّمة بتعليقات `BINDING:` بالكود (`EditorCapture.cpp`, `AutoDeco.cpp`, `Main.cpp`) لأنها ممكن تختلف شوي حسب نسخة Geode SDK بالضبط — تصحيحها سريع لما تعرف رسالة الخطأ.

## قبل الاستخدام الفعلي

عبّي IDs الحقيقية لكائنات الديكور بأعلى `src/AutoDeco.cpp` (مكتوب عليها `TODO: verify real ID`) — خذها من المحرر نفسه أو من قائمة IDs الرسمية للعبة، مو من أي مود ثاني.
